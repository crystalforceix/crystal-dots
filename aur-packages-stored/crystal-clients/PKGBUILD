# crystal-clients

# Maintainer: crystalforceix@gmail.com

pkgname='crystal-clients'
pkgver=1.0
pkgrel=1
pkgdesc='a main packages for dotfiles also niri and quickshell'
arch=('any')
url='https://github.com/crystalforceix/crystal-dots'
license=('GPL-3.0-only')
depends=('niri' 'quickshell-git' 'xwayland-satellite'
	 'fuzzel' 'brightnessctl' 'pamixer'
	 'bluez' 'bluez-utils' 'blueman'
 	)
optdepends=('spotify: music player'
            'vscode: code editor'
            'zen-browser: web browser'
            'todoist-appimage: todo manager'
            'uwsm: uwsm session'
            'gnome-keyring: keyring daemon'
            'polkit-gnome: gnome polkit client')

